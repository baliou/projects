{% extends 'base.html' %}
{% block title %}Log in{% endblock %}
{% block extrastyle %}
{% endblock %}
{% block content %}

<div class="container-fluid"> 
	<div class="row-fluid">
		 <div class="row jumbotron">
		  <div class="col-xs-6">
				<fieldset>
				<legend><b> Créer votre entreprise </b> </legend>
				  <form action="/create/" class="form-horizontal" role="form" method="post">{% csrf_token %}
					{% for field in form %}
										  {% for error in field.errors %}
										  <div class="alert alert-danger">{{ error }}</div>
											
										  {% endfor %}
						  <div class="form-group">
							<label for="name" class="col-sm-2 control-label">{{ field.label_tag }}</label>
							<div class="col-sm-10">
							  {{ field }} 
							</div>
						  </div>
					{% endfor %}
						<button type="submit" class="btn btn-warning pull-right">
							<span>Créer votre boutique</span>
							<i class="glyphicon glyphicon-ok"></i>
							
						</button>
				  </form>
				</fieldset>	  
		  </div>
		  <div class="col-xs-6 ">
			<fieldset style="border-radius: 5px; padding: 5px; min-height:150px;">	
		<legend><b> Bienvenue chez 360 </b> </legend>   
		   <div id="myCarousel" class="carousel slide" data-interval="3000" data-ride="carousel">
				<!-- Carousel indicators -->
				<ol class="carousel-indicators">
					<li data-target="#myCarousel" data-slide-to="0" class="active"></li>
					<li data-target="#myCarousel" data-slide-to="1"></li>
					<li data-target="#myCarousel" data-slide-to="2"></li>
				</ol>   
			   <!-- Carousel items -->
				<div class="carousel-inner">
					<div class="active item">
						<h2>Entreprise</h2>
						<div class="carousel-caption">
						  <h3>Factures et Devis</h3>
						  <p>Gestion de devis et de factures , la plus simple possible.</p>
						</div>
					</div>
					<div class="item">
						<h2>Produits</h2>
						<div class="carousel-caption">
						  <h3>Lister tous vos produits</h3>
						  <p>Pour aller plus vite.</p>
						</div>
					</div>
					<div class="item">
						<h2>Bilan</h2>
						<div class="carousel-caption">
						  <h3>Des options en bonus</h3>
						  <p>Voir les bénéfices sur une période , permettre à vos clients de voir leurs factures en lignes.</p>
						</div>
					</div>
				</div>
				<!-- Carousel nav -->
				<a class="carousel-control left" href="#myCarousel" data-slide="prev">
					<span class="glyphicon glyphicon-chevron-left"></span>
				</a>
				<a class="carousel-control right" href="#myCarousel" data-slide="next">
					<span class="glyphicon glyphicon-chevron-right"></span>
				</a>
			</div>
		  </fieldset>	
		  </div>
			
		 </div> 
			 

	</div>
</div>
{% endblock %}
